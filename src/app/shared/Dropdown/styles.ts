import { Theme } from '@mui/material';
import { SxProps } from '@mui/material';

export const SxDropdown: SxProps<Theme> = {
  '& .MuiSelect-icon': {
    transition: '.2s all ease-in !important',
  },
};
